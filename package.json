{
  "name": "spatial-span-assessment",
  "version": "0.1.0",
  "description": "Corsi Block-Tapping (spatial span) assessment prototype - seeded stimuli, trial-level logging, norm gating, automated-decision explanation",
  "private": true,
  "scripts": {
    "test": "node tests/logic.test.js && node tests/task.test.js && node tests/nback.test.js && node tests/wire.check.js && node tests/interview.test.js && node tests/interview.wire.js"
  },
  "license": "MIT",
  "dependencies": {
    "@anthropic-ai/sdk": "^0.124.0",
    "zod": "^4.6.1"
  }
}
